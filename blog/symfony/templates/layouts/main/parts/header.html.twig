<b-navbar toggleable="md" type="dark" variant="info">

  <b-navbar-toggle target="nav_collapse"></b-navbar-toggle>

  <b-navbar-brand href="/">
    {% trans %}
      Blog
    {% endtrans %}
  </b-navbar-brand>

  <b-collapse is-nav id="nav_collapse">

    <b-navbar-nav>
      <b-nav-item href="/">
        Home
      </b-nav-item>
      <b-nav-item href="{{ path('blog') }}">
        Blog
      </b-nav-item>
    </b-navbar-nav>

    <!-- Right aligned nav items -->
    <b-navbar-nav class="ml-auto">

      <b-nav-form>
        <b-form-input size="sm" class="mr-sm-2" type="text" placeholder="Search"/>
        <b-button size="sm" class="my-2 my-sm-0" type="submit">Search</b-button>
      </b-nav-form>

      <b-nav-item-dropdown text="Lang" right>
        <b-dropdown-item href="/">
          EN
          <img src="https://www.countryflags.io/us/shiny/32.png">
        </b-dropdown-item>
        <b-dropdown-item href="/ru">
          RU
          <img src="https://www.countryflags.io/ru/shiny/32.png">
        </b-dropdown-item>
      </b-nav-item-dropdown>

      {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <b-nav-item-dropdown right>
          <template slot="button-content">
            <em>👤 {{ app.user.username }}</em>
          </template>
          <b-dropdown-item href="{{ path('profile') }}">
            Profile
          </b-dropdown-item>
          {#TODO не уверен что правильное сгенерировал роут, есть еще logout_url('main')#}
          <b-dropdown-item href="{{ path('app_logout') }}">
            Logout
          </b-dropdown-item>
        </b-nav-item-dropdown>
      {% else %}
        {#TODO не уверен что правильное имя роута (нужна сраница /login)#}
        <b-nav-item href="{{ path('app_login') }}">
          Sign In
        </b-nav-item>
        <b-nav-item href="{{ path('app_register') }}">
          Sign Up
        </b-nav-item>
      {% endif %}
    </b-navbar-nav>

  </b-collapse>
</b-navbar>
