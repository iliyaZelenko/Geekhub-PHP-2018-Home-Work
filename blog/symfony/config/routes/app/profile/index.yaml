# Auth
profile:
    path: /
    controller: App\Controller\ProfileController::index
    methods: [GET]

profile_posts:
    path: /posts/{page}
    controller: App\Controller\ProfileController::posts
    requirements:
        page: '\d+'
    defaults:
        page: 1
    methods: [GET]
