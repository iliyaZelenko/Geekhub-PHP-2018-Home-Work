Практиковал паттерны.

Чтобы открыть результат нужного паттерна выполните `composer serve` с папкой паттерна, например:

```sh
composer serve Command
```

или

```sh
composer serve FactoryMethod/NewVariant
```
